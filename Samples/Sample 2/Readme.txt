This sample contains two folders.

.\SQL-CRM - Daily\XmlSynchro.Run.exe is scheduled to run every day at 1:00am (the execution is triggered by the Task Scheduler).

.\SQL-CRM - Hourly\XmlSynchro.Run.exe is scheduled to run every hour starting at 00:15am (the execution is triggered by the Task Scheduler).